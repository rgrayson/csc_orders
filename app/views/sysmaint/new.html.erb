<h1>Menu Header Maintenance</h1>

<h2>Existing Menus</h2>
<table>
  <tr>
    <th>Menu</th>
    <th>Wh Del Dte</th>
    <th>Status</th>
  </tr>
 
   <% @t.each do |tbl| %>
    <tr>
      <td><%= tbl.mcode %></td>
      <td><%= tbl.dte %></td>
      <td><%= tbl.sts %></td>
      <td><%= link_to 'Remove', sysmaint_path(id: tbl.id),
              method: :delete,
              data: { confirm: 'Are you sure you want to delete this record?' } %></td>
      <td><%= link_to 'Line Detail', sysmaintli_path(id: tbl.id, p1: tbl.mcode,
                       p2: tbl.dte, p3: tbl.sts) %> </td></TD>          
    </tr> 
  <% end %>
</table>

<%= form_for :menu, url: sysmaint_index_path do |f| %>    
    <p>
        Menu: <%= f.text_field :mcode %>
        Wh Del Date: <%= f.date_field :dte %>
        Status: <%= f.text_field :sts %> 
        
    </p>
    <p><%= f.submit %></p>
<% end %>


<%= link_to 'Back', sysmaint_index_path %>

